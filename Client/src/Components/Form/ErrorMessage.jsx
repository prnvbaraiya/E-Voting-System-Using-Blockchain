import { Typography } from "@mui/material";

export const ErrorMessage = () => {
  return (
    <Typography
      variant="inherit"
      sx={{ display: "none" }}
      color="textSecondary"
    >
      Error
    </Typography>
  );
};
